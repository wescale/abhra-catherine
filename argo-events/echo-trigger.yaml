---
apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: echo-sensor
  namespace: tools-argo-events
spec:
  template:
    serviceAccountName: argo-events-sa
  dependencies:
    - name: message-body-cd
      eventSourceName: k8s-event
      eventName: argo-cd-secret
    - name: message-body-workflows
      eventSourceName: k8s-event
      eventName: argo-workflows-secret
  triggers:
  - template:
      name: echo-body
      k8s:
        group: ""
        version: v1
        resource: pods
        operation: create
        source:
          resource:
            apiVersion: v1
            kind: Pod
            metadata:
              generateName: echo-payload-
              labels:
                app: echo-payload
            spec:
              containers:
              - name: echo-payload
                image: eu.gcr.io/weshare-333609/bzh/alpine:latest
                command: [echo]
                args: [""]
              restartPolicy: Never
        parameters:
        - src:
            dependencyName: message-body-cd
            dataTemplate: "{{ .Input.body.data.password | b64dec }}"
          dest: spec.containers.0.args.0
        - src:
            dependencyName: message-body-workflows
            dataTemplate: "Bearer {{ .Input.body.data.password | b64dec }}"
          dest: spec.containers.0.args.0

          
